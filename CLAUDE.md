# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project Overview

AmneziaWG userspace VPN (the `amneziawg-go` daemon + `awg` CLI) + web UI addon for ASUS routers running Asuswrt-Merlin (384.15+, including the 3006.x / `3.0.0.6` BE-series builds) firmware. Provides DPI-obfuscated WireGuard VPN with per-device policy routing and GeoIP/GeoSite selective routing. All documentation and UI text is in Russian.

**Target:** ARM64 (aarch64) and ARM32 ASUS routers. Fully userspace — no kernel module, works on any kernel version (only the stock `tun` device is needed).

## Build

Pure userspace — **no kernel module**. Packages are built and published by CI on tag push (`.github/workflows/release.yml`):
- `amneziawg-go` daemon built with Go (`GOARCH=arm64`, plus `arm` GOARM=7); the **GOARM=5 daemon for `armv7-2.6` is a separate LEGACY build** — pinned Go 1.23 toolchain + downgraded deps (x/sys v0.35, x/net v0.43, x/crypto v0.41), because Go ≥ 1.24 doesn't support the 2.6.3x kernels those routers run; it reports `--version` as `v0.2.19-legacy26-pool1024-smfix (…)`. **The daemon is built from OUR FORK (`william-aqn/amneziawg-go`, branch `router-build` = v0.2.19 + two patch commits), not upstream** (since 1.2.58) — the `-pool1024` suffix = the bounded-buffer-pool patch, `-smfix` = the sendmmsg→sendmsg ENOSYS fallback (the fix that lets 2.6.x kernels pass traffic at all),
- `awg` CLI built as a **static-musl** binary per arch (Alpine for aarch64/armv7; `dockcross/linux-armv5-musl` **soft-float** for VFP-less Cortex-A9 like RT-AC68U),
- `./build-ipk.sh` assembles the `.ipk`s into `output/`.

Build locally by replicating those steps (or run `./build-ipk.sh` once the binaries are in `output/`). Install on a router with `install-online.sh` (`curl … | sh` → `opkg`) or `opkg install <pkg>.ipk`.

## Architecture

### Build pipeline (`.github/workflows/release.yml`)
Userspace-only: builds `amneziawg-go` (Go) and the static-musl `awg` CLI per architecture, then `build-ipk.sh` packs them into `.ipk`s. On a `v*` tag the workflow publishes a GitHub Release (notes pulled from the matching `CHANGELOG.md` section) and registers the tag with jsDelivr (the router resolves updates via jsDelivr when api.github.com is region-blocked).

### Router-side components

**`addon/amneziawg.sh`** — Main backend script (runs on router). Handles:
- Interface lifecycle: `start`/`stop`/`restart` (launch `amneziawg-go` userspace daemon, ip link, awg setconf, iptables, ip rule)
- Config generation from Merlin's `custom_settings.txt` (key prefix: `awg_*`)
- Per-device routing policy: `vpn_all`, `vpn_geo`, `direct` via ip rules + iptables mangle marks
- GeoIP/GeoSite: downloads country CIDR lists, domain lists; populates ipset (`awg_dst`) + dnsmasq ipset rules
- Web UI addon mounting via Merlin Addons API (`am_get_webui_page`, menuTree.js bind mount)
- Service event dispatch (called from `/jffs/scripts/service-event`)

**`addon/amneziawg_page.asp`** — Web UI page (ROG-styled ASP). Communicates with backend via Merlin's `httpApi` custom settings and service events (`awgstart`, `awgstop`, `awgsaveconf`, `awgupdategeo`). Reads status from `/www/user/awg_status.htm` (JSON).

**`addon/amneziawg_server.sh`** — AWG **SERVER role** (since 1.3.0): inbound road-warrior access + per-peer policy (double hop through the client tunnel). Sources `amneziawg.sh` in lib mode (`AWG_LIB_MODE=1` guard sits right above the client's main dispatch) and overrides the instance globals (`IFACE=awgs0`, `LOCKDIR=/tmp/.awgs_lock`, `UI_LOG`/`STATUS_FILE`/`DAEMON_LOG`/`DAEMON_RC`, `SCRIPT_NAME=awg-server`) — so every hardened shared helper operates on the server instance. Key invariants:
- **The server daemon runs under its own process name `awgs-go`** (hardlink to amneziawg-go, recreated each start — a package update changes the inode). Client paths do blanket `kill $(pidof amneziawg-go)`/`wait_for_pid_exit amneziawg-go`; a second daemon under that name would die on every client stop/start. Never launch the server daemon under the stock name.
- Settings prefix `awgs_` (NOT matched by `^awg_` — 4th char). Peer store `awgs_peers` (+`awgs_peers1..N` chunks): `name|ip|policy|mode|enabled|pubkey|privkey|psk` entries `;`-joined. Server obfuscation params are its own set (`awgs_jc`…, chunked `awgs_initdata*` for I1-I5) — unrelated to the client's provider params.
- **Per-peer policy rides the CLIENT's engine**: `server_peer_policy_entries` (in amneziawg.sh) appends peer IPs to `clients.list` in `save_clients`; the server pokes `amneziawg.sh apply_policies` on changes. Rules die with the client firewall ⇒ peers **fail OPEN** to WAN when the client tunnel is down (documented; page shows a yellow banner). Peer subnet is mangle-RETURNed + hairpin-routed into table 300 so vpn_all sources still reach peers directly.
- **Peer DNS via the postconf hook, never an include**: `interface=awgs0` is pc_appended by `/jffs/scripts/dnsmasq.postconf` ONLY while `/sys/class/net/awgs0` exists — a dangling directive after a crash is impossible (the conf-file= brick lesson). Server start/stop clears `DNSRELOAD_SIG` before `reload_dnsmasq`, else the md5-skip would swallow the needed restart.
- No CTF gate (server adds no ip rules/fwmark — the exact CTF-wedge machinery) and no health-check rollback (a listener can't lock out the LAN). `awgs_autostart` defaults OFF (a WAN-port-opening server must be opt-in); `finalize_ipk_install` auto-restarts a running server after updates (the remote peer updating through it must not be locked out). Events `awgsrv*` forward from the client's `do_service_event`; UI page keys/QR are generated in-browser (`awg_qr.js`: qrcodegen port + Curve25519, RFC 7748-verified — private keys never transit the backend).

**`install-online.sh`** — On-router installer (`curl … | sh`): resolves the latest release (GitHub API → jsDelivr fallback), downloads the arch-matched `.ipk`, verifies its SHA256, and `opkg install`s it. The `.ipk` (built by `build-ipk.sh`) ships the `S99amneziawg` CLI entry-point and its postinst registers the addon page.

### Key paths on router
- `/opt/amneziawg/` — daemon (`amneziawg-go`), tool (`awg`), config, client list, geo data
- `/jffs/addons/amneziawg/` — addon script + ASP page
- `/jffs/configs/dnsmasq.conf.add` — holds a persistent `conf-file=/opt/amneziawg/dnsmasq_awg.conf` include line; the actual domain→ipset rules live in that included file (header `# AmneziaWG domain routing`), NOT in conf.add itself
- `/jffs/scripts/dnsmasq.postconf` — one tagged line (marker `amneziawg`): while `/tmp/.awg_tunnel_dns` exists it `pc_delete`s the firmware's `servers-file`/`resolv-file` from the generated dnsmasq conf (the "DNS via tunnel" feature)
- `/jffs/addons/custom_settings.txt` — Merlin settings store (all keys prefixed `awg_`)

### Routing model
Three policies per device: `vpn_all` (ip rule → table 300), `vpn_geo` (iptables fwmark 0x100 + ipset match → table 300), `direct`. Default policy applies to unlisted devices. Route table **300**, fwmark 0x100; the `ip rule`s sit at distinct priorities by type — 97 (direct → main), 98 (fwmark → 300, the geo mechanism), 99 (vpn_all → 300), 100 (router-originated → 300). (Table is 300, not 200 — bumped to avoid colliding with co-resident NFQUEUE DPI tools like b4; see coexistence notes below.)

## Shell scripting notes

All router-side scripts must be POSIX sh (busybox ash) — no bashisms. The router runs BusyBox with limited coreutils. Specific gotchas, each of which has bitten us:

- **No `command -v`** — Merlin busybox lacks `CONFIG_ASH_CMDCMD`, so `command -v` returns 127 and silently disables any guard built on it. Use `which`.
- **`custom_settings.txt` caps a SINGLE value at ~3000 chars** — the firmware (via `<% get_custom_settings() %>`) silently truncates the overflow, no error. Any setting that can exceed ~2900 chars (e.g. a long `I1`-`I5` base64 — a big `<b 0x…>` junk packet) MUST be chunked across multiple keys (`awg_initdata`, `awg_initdata1`, …) and reassembled identically in the page (load + save) AND in `generate_config`. Don't add a new long single-key setting without chunking. busybox `awk` itself reads 3400-char lines fine — it's not the limiter.
- **Entware binaries with versioned `.so` deps** (e.g. `ipset` → `libipset`) load the firmware's older `/usr/lib` copy when run from the addon context and die at link time. ROOT CAUSE (found 1.2.46): the firmware `httpd` exports `LD_LIBRARY_PATH=/lib:/usr/lib` and every child inherits it (`httpd → service-event → the addon → /opt/bin/*`); recent Entware ELF binaries bake `/opt/lib` into `DT_RUNPATH` (searched AFTER `LD_LIBRARY_PATH`), so the firmware glibc wins and grep/sed/awk/ipset SIGSEGV on start — while the SAME binary works from an SSH login (no poisoned var). **The script now does `unset LD_LIBRARY_PATH` at the very top** (fixes it; firmware binaries default-search `/lib:/usr/lib` so they're unaffected; static `awg`/`amneziawg-go` were always immune). Two TUF-AX3000_V2 users reformatted USBs for nothing before this. Diag prints the inherited `LD_LIBRARY_PATH` + a 3-env `/opt/bin/grep` probe.
- **`base64` may be absent** — some firmware busybox builds omit the applet and the box may have no Entware coreutils `base64` either. Don't assume it exists.
- **Cyrillic is everywhere** in this repo (RU UI/docs) — search with `rg` (ripgrep), not `grep`.
- Capture `setconf`/command **stderr** on failure (it used to be discarded); a bare numeric exit code is rarely actionable.
- **A present-but-0-byte binary is a real failure mode** — an interrupted opkg update on a low-RAM box (power-cycle mid-write, then e2fsck truncation on the USB where /opt lives) left `amneziawg-go`/`awg` at 0 bytes; running an empty file "exits rc=0" and looks like "failed to create interface". `do_start` now validates existence+non-empty+executable before launch; `finalize_ipk_install` re-verifies non-empty after opkg and force-reinstalls once (1.2.41). Test `[ -s "$bin" ]`, not just `[ -f ]`.
- **Entware coreutils can be CORRUPTED (dying USB) and segfault** — /opt/bin/grep shadowing busybox took down a LAN in the field (`grep -qF` gates read "not present", the br0-IP check read "no network", the dnsmasq include never got stripped → dnsmasq fatal). The script self-tests grep/sed/awk at startup and falls back to the firmware-busybox PATH (`AWG_PATH_SANE=0`, warning logged once per boot, visible in diag). Don't build new LAN-critical guards on Entware-shadowable tools without remembering this fallback exists.
- **iptables rule adds must be `-C`-guarded and teardown must DRAIN all copies** (`ipt_drain`) — `iptables -D` removes one copy per call, and duplicates accumulated in the field (9× TCPMSS) via a double-start race until 1.2.31 closed it (`is_running` is re-checked under the lock in `do_start`).
- **`ip rule add` has NO idempotent form** (no `-C`/replace) — so every add MUST go through `ip_rule_replace` (drain all copies of that exact rule, then add one) and teardown/`cleanup_firewall` drains all copies at our fixed priorities 97-100 via `drain_ip_rules`. Field-seen: prio 97/98/99/100 each DOUBLED (1.2.48 fix) because `setup_firewall` runs a SECOND pass once domain-geo finishes downloading (pass 1 has 0 domains) and re-added every `ip rule` on top of pass 1's — harmless to routing (identical rules) but grows unbounded across restarts. Don't reintroduce a bare `ip rule add`.
- **`ip rule del <partial-selector>` with NO match does NOT fail on Broadcom 2.6.36 + Entware iproute2 — it deletes a DIFFERENT (non-matching) rule** (same RTM-netlink mismatch family as the 1.2.40 `ip link show` false-negative). So a blind delete-while-success loop (`while ip rule del prio 97; do …`, or `ip rule del lookup 300` / `del fwmark 0x100`) DRAINS THE WHOLE TABLE — system `local`/`main`/`default` included — leaving the router **"Network is unreachable"**: no egress, so the tunnel handshake can't even leave (`awg show` shows TX>0 RX=0), the tunnel never comes up, and the box reboots. **This was THE root cause of "the full start bricks the RT-AC68U"** (found + fixed 1.2.61; it masqueraded for days as CTF, then rp_filter, then "resource pressure"). Fix: `drain_ip_rules`/`ip_rule_replace` NEVER blind-loop `ip rule del` — they enumerate `ip rule show` (RTM_GETRULE lists correctly on this kernel), pick only OUR rules (priority band 97-100 + our table/fwmark), and delete each by its confirmed-present priority; system rules (0/32766/32767) are outside the band and can't be selected. `ip rule del prio N` for an EXISTING, ours N is fine — it's only the NO-match case that mis-deletes. Don't reintroduce a bare `ip rule del <partial>` in a success-loop.
- **`$$` inside a `( ) &` subshell is the PARENT's pid (POSIX)** — lock pid-files written with `echo $$` from detached subshells (reload jobs, ensure_geo, health-check) recorded a pid that dies in seconds, so every liveness probe misread live locks as stale and STOLE them (field-confirmed on 1.2.33). Write lock pids with `sh -c 'echo $PPID' > pidfile` (direct child + plain redirect — no command substitution, whose extra fork makes the answer ambiguous) and compare ownership against the read-back value (`AWG_LOCK_PID`), never against `$$`.

## amneziawg-go daemon & setconf

- **The daemon is built from OUR FORK, not upstream (since 1.2.58):** `release.yml` clones `AWG_GO_REPO=william-aqn/amneziawg-go` at `AWG_GO_REF=router-build` — a branch based on the `AWG_GO_TAG=v0.2.19` tag (kept for reproducibility + the AWG 1.5/2.0 params `I1`-`I5`/`S3`/`S4` + "handle empty I1-I5" fix) plus **two patch commits**: bounded buffer pool (`PreallocatedBuffersPerPool = 1024`, OOM fix) and the sendmmsg→sendmsg ENOSYS fallback (2.6.x traffic fix). Both were CI seds / unbuilt before; now real commits. CI still **HARD-asserts** both are present (`grep -q 'PreallocatedBuffersPerPool = 1024'` + `grep -q 'batchUnsupported'`) — a bad rebase or wrong `AWG_GO_REF` fails the build. Once upstream merges the PRs, point `AWG_GO_REPO` back to `amnezia-vpn` + drop the asserts. The build still **sed-patches `version.go`'s `const Version`** (ldflags `-X` can't override a `const`) so `--version`/diag report the real ref + patch markers; upstream hardcodes a stale `const Version = "0.0.20250522"` — **do not infer the daemon's age from that string**.
- **The "Running amneziawg-go is not required because this kernel has first class support" box is a HARMLESS, UNCONDITIONAL wireguard-go notice** printed to stderr on every Linux start — never the cause of a failure. Read the journal line *after* it.
- **`setconf failed … "Unable to modify interface: Invalid argument"` = the daemon rejected an obfuscation param (EINVAL), not a race.** UAPI returns only a numeric errno (→ `awg` prints the generic string) and the default log level is silent on the reason. `do_start` re-launches the daemon under `LOG_LEVEL=verbose` + one `setconf` so it NAMES the line (e.g. `failed to parse I1: missing enclosing >`) and dumps the non-secret advanced params. Daemon constraints: `jc/jmin/jmax > 0`, `s1-s4 ≥ 0`, `h1-h4` must not overlap, `i1-i5` parsed strictly. `H1=1 H2=2 H3=3 H4=4` + `S=0` is VALID (I-param mode, e.g. a Cloudflare-WARP config).
- **setconf UAPI race**: amneziawg-go creates the `awg0` netdev BEFORE it binds the UAPI control socket, so an immediate `setconf` (right after `wait_for_iface` passes) can lose the race and die with a generic exit 1 even on a fast box. `wait_for_uapi` (probes via `awg show`, path-agnostic) + retries cover it.
- **Post-start health check (≈60s auto-rollback)**: after `do_start` brings up the daemon + firewall, a backgrounded probe pings the configured hosts THROUGH the tunnel (`ping -I awg0`, default `8.8.8.8`/quad9, 30×2s). If nothing passes within ~60s it auto-rolls-back (`do_stop`) to prevent lockout, after capturing `awg show` (handshake age / transfer) into the journal — so "up but no traffic" is diagnosable: handshake present → fault is downstream (route/MTU/egress hijacked); no handshake → endpoint unreachable or obfuscation mismatch. Probe hosts come from `awg_watchdog_hosts` (sanitized against shell injection). The same probe drives the periodic watchdog.
- **DNS failure NEVER rolls back / restarts a tunnel that passes traffic (since 1.2.31)** — when the ICMP/TCP probe passes but the DNS gate fails, both the health check and the watchdog **fail OPEN**: they remove OUR `:53` DNAT (`cleanup_dns_interception`) and keep the tunnel up (a VPN restart can't fix a broken resolver; the client lockout is cured by dropping the DNAT). The watchdog reconciler re-installs interception only when `intercept_wanted && dns_ok` — the `dns_ok` gate prevents a 5-min install/fail-open flap. Don't reintroduce "DNS dead → restart VPN".
- **The daemon is launched via `launch_daemon` with `WG_PROCESS_FOREGROUND=1`** (wrapper writes the rc to per-launch `/tmp/awg_daemon.rc` + appends `[daemon exited rc=N]` to the log) — a silent signal death (132=SIGILL, 139=SIGSEGV) is distinguishable from a hang; interface-create failure retries once under `LOG_LEVEL=verbose` and continues if the retry succeeds. WITHOUT the foreground env, amneziawg-go daemonizes and the wrapper records the fork-PARENT's rc=0 on every successful start (live-confirmed footgun, fixed 1.2.36). Keep new launch paths going through `launch_daemon`.
- **`launch_daemon` sets `GOMEMLIMIT` + `GOGC=50` on the daemon (since 1.2.47) — the fix for "tunnel collapses under heavy load".** amneziawg-go inherits wireguard-go's UNBOUNDED message-buffer pool (`PreallocatedBuffersPerPool = 0`), so a heavy INBOUND burst grows the Go heap faster than it drains; with default `GOGC=100` the heap can DOUBLE before a GC, and on a low-RAM 32-bit box (field: RT-AX82U 512MB, no swap) the kernel refuses the next arena page → the runtime aborts the WHOLE daemon with `fatal error: runtime: out of memory` (rc=2), watchdog crash-loops it. Light traffic never trips it — only serious load. `compute_go_memlimit()` computes a soft ceiling ≈55% of `/proc/meminfo` MemAvailable→MemFree at launch, clamped [64,448]MiB; the SOFT limit makes GC reclaim the pool's idle 64KB buffers (they're ordinary GC garbage) and, if the genuine live set needs more, EXCEEDS the limit (Go caps GC at 50% CPU, no death-spiral) rather than hanging — so it's never worse than the unbounded status quo. Env is `export`ed INSIDE the `( )&` subshell (not a literal prefix). OOM is recorded to the persistent incident log (`record_daemon_oom` greps the daemon log for `out of memory` — precise: an intentional SIGTERM/SIGKILL never prints it) and flagged in diag. **This is a REAL daemon OOM — do NOT conflate it with the 1.2.42 dangling-dnsmasq-`conf-file` LAN brick (different box/symptom, and its changelog said "не OOM"); gate on the actual `out of memory` string.** GOMEMLIMIT alone was field-proven INSUFFICIENT (1.2.48: settled box, correct 190MiB ceiling, still OOM'd ~4 min into YouTube — the ceiling can't reclaim buffers LIVE in the queues); **the real cure shipped in 1.2.50: `PreallocatedBuffersPerPool = 1024`** (upstream's own iOS/Windows memory-constrained profile; `WaitPool.Get` blocks when full = true backpressure; pool tops out at 1024×64KB=64MB; kernel drops excess UDP = normal congestion signaling). Was a `release.yml` sed 1.2.50–1.2.57; **since 1.2.58 it's a commit on the fork's `router-build` branch** (see the Build section). Both builds (main + legacy26) HARD-assert it — a silent unbounded pool cannot ship. **Do NOT shrink `MaxSegmentSize` instead**: kernels ≥4.18 deliver UDP-GRO-coalesced datagrams up to 64KB (fleet has 4.19 — TUF-AX3000_V2) and a smaller buffer would truncate them; the pool cap is size-agnostic. Patched daemons self-identify: `--version` → `v0.2.19-pool1024-smfix (…)` / `v0.2.19-legacy26-pool1024-smfix (…)`.
- **amneziawg-go v0.2.19 requires Go 1.24.4 (go.mod), and Go ≥ 1.24 supports only Linux kernel ≥ 3.2** — on the RT-AC68U's 2.6.36 (all 386.x builds) the stock daemon is officially unsupported and dies unpredictably/silently (banner prints, then nothing — the rc line above is how it shows). **Since 1.2.32 the `armv7-2.6` package ships a LEGACY daemon built with Go 1.23** (Linux floor 2.6.32): only x/sys→v0.35.0, x/net→v0.43.0, x/crypto→v0.41.0 need downgrading — the outline-sdk chain that hard-requires go 1.24 lives in the mobile-only `./outline` subpackage which is NOT linked into the daemon binary. Verified on a real 2.6.32 kernel (qemu-system-arm versatilepb + Debian Squeeze armel; recipe: images from people.debian.org/~aurel32/qemu/armel/, payload via genext2fs second disk, expect over serial): TUN create + obfuscated setconf + UAPI pass. The legacy daemon RUNS on the AC68U (field-confirmed 1.2.39: full verbose init, `awg0` created) — the remaining 1.2.40 issue was interface DETECTION, not the daemon.
- **Detect interface existence via `/sys/class/net`, NEVER `ip link show`** — amneziawg-go names the netdev `awg0` directly via `TUNSETIFF` (proven by the daemon's own successful `SIOCGIFINDEX`), so the device is registered in the kernel; but a mismatched Entware iproute2 on an old kernel (RT-AC68U 2.6.36) fails `ip link show awg0`'s `RTM_GETLINK` and false-negatived a LIVE tunnel for 10s, so `do_start` killed the daemon as "failed to create interface" (fixed 1.2.40). `iface_exists()` reads `/sys/class/net/$iface` (→ `/proc/net/dev` → `ip` last) and backs `is_running`/`wait_for_iface`/watchdog/fw-vpn detection. The `ip addr/route/rule/link set` setup commands use `RTM_NEW*`/`SETLINK` (stable across kernels — the daemon itself brought the iface up that way), so they were never the problem.
- **A kernel-2.6.x box (RT-AC68U) can create the interface + setconf but CANNOT PASS TRAFFIC — the daemon sends UDP via `sendmmsg()`, which is ENOSYS before Linux 3.0** (found 2026-07-12 by a WAN-safe daemon-only handshake test; verbose log: `Failed to send handshake initiation: write udp …: sendmmsg: function not implemented`). Handshake/keepalive packets are FORMED but never egress → no handshake → health-check "not passing traffic" rollback, on ANY config. **THIS is why AWG never actually worked on the AC68U** — not CTF, not routing, not the endpoint; those were real but secondary (CTF = a separate hard-wedge when policy routing comes up; see the CTF note in coexistence). Every prior "AC68U works" note (1.2.39/1.2.40) only proved interface-create + setconf + UAPI — NEVER traffic. `awg show` even echoes the h1-h4 ranges + I1 back, which misleads you into thinking config is the problem. **FIXED (1.2.58): the fork's `router-build` branch carries a `conn/bind_std.go` patch — a sticky `batchUnsupported` flag that falls back to per-packet `WriteMsgUDP`/`ReadMsgUDP` when `WriteBatch`/`ReadBatch` return ENOSYS** (both sendmmsg AND recvmmsg). Proven on a real RT-AC68U: handshake completes, `transfer` RX/TX > 0 both ways. The `-smfix` version suffix marks it. The smfix daemon was NECESSARY but not the whole story — the full `do_start` still bricked the AC68U (`transfer: 0 B received, 3xx B sent`, box rebooted) for a SECOND, independent reason that masqueraded as CTF then rp_filter then "resource pressure": **the blind `ip rule del` drain wiped the system routing table** (see the `ip rule del` gotcha in Shell notes). **FULLY RESOLVED 1.2.61** (drain_ip_rules/ip_rule_replace enumerate + delete only our rules): verified end-to-end on a real RT-AC68U — system rules survive, handshake completes, RX/TX both flow, inbound stays up, no reboot, health-check passes. **The 2.6.x `kernel_pre_sendmmsg` guard/banner was REMOVED in 1.2.61** — 2.6.x is supported again (needs the `-smfix` daemon, in the .ipk since 1.2.58, + CTF off first if present). So on the AC68U, in order: CTF off (ctf guard/banner) → smfix daemon (bundled) → the ip-rule-drain fix (1.2.61) → tunnel works.
- **Watchdog liveness**: `do_watchdog` writes a heartbeat to `/tmp/.awg_wd_beat` every tick (diag prints it) and RECLAIMS a stale `/tmp/.awg_lock` whose holder pid is dead — a leaked lock used to disable self-heal silently, forever (field case: hours of journal silence while the tunnel was down). Any new long-running op must write its pid into `$LOCKDIR/pid` (acquire_lock does) or the watchdog will treat its lock as stale after 5 min.
- **The watchdog must stand down during an update** — `finalize_ipk_install` intentionally stops the VPN (non-user `do_stop`, so the `awg_watchdog` cron is NOT removed — only a `do_stop user` drops it). Without a guard the `*/5` watchdog saw the stopped `awg0` and raced a restart against the installer (field-reported: "auto-update kills the connection, the watchdog brings it back up, then it gets removed and reinstalled", 1.2.48 fix). `finalize_ipk_install` sets `/tmp/.awg_no_autostart` BEFORE its first teardown (the "commit point", after the opkg-runnable guard) and clears it on every exit path; `do_watchdog` early-returns while the flag is fresh, but RECLAIMS it if older than 15 min (leaked by a died updater — else it would disable self-heal forever, like the stale-lock case). The same flag gates `do_start` (the opkg-triggered S99). Don't set `no_autostart` only around the opkg call — it must cover from the first `do_stop`.

## DNS, firewall & geo

- **A `service restart_dnsmasq` issued while rc_service is processing one of OUR OWN service-events is LOST, not queued** — notify_rc blocks ~15s waiting for the in-flight event (our own handler → circular wait) and then DROPS the request (`skip the event: restart_dnsmasq` in syslog; field update log: 7 straight drops at ~18s cadence, two ~1-min stalls from the 60s lock-settle waits, then a restart tail after the event ended). Since 1.2.53 reload jobs poll `nvram get rc_service` and fire only when rc is idle (`_rc_settle`, job-global 150s budget, pre-fire `_reload_took` re-check), and `finalize_ipk_install` coalesces the whole stop→prerm→postinst→stop reload storm into ONE deferred reload via `/tmp/.awg_dnsreload_defer`/`_pending` (TTL 15 min like `.awg_no_autostart`; `dnsreload_defer_end` fires it on every post-commit exit path). Don't add bare `service restart_dnsmasq` calls on paths reachable from a service-event — go through `reload_dnsmasq`.
- **`:53` DNAT (DNS interception) must be installed AFTER the dnsmasq restart, never before** — installing it first locks every LAN device out of DNS/DHCP until reboot (LAN-critical). Check `setup_dns_interception` vs `reload_dnsmasq` ordering.
- **Never flush conntrack by `--mark`** — our fwmark is per-PACKET (mangle MARK, deliberately no CONNMARK) so it never lands in the conntrack mark, while AiProtection/QoS firmwares set their OWN ctmarks that often carry bit 0x100 (tdts marks like 195040=0x2F9E0): a `-D --mark 0x100/0x100` cut random established LAN sessions on every Apply and dumped each deleted entry to stdout (live-confirmed on GT-AX6000, fixed 1.2.35). `flush_conntrack` deletes by SOURCE IP of explicitly VPN-policied clients only, quietly.
- **dnsmasq honors only the FIRST `ipset=` line per domain.** Domains shared across multiple geo policies must be ONE comma-joined `ipset=/dom/setA,setB` line, not separate per-policy lines.
- **Domain-geo effectively requires DNS interception** — without the :53 DNAT, domains populate the sets only for clients that voluntarily use the router's dnsmasq (fresh installs default to compat mode = interception OFF → "ничего не работает" reports). Status field `dnsgeo_warn` ("user" | "dpi:<tool>" | "fwdns:<owner>", DoT and mid-start excluded) drives a yellow page banner when domain rules exist + geo is routed + tunnel runs + DNAT absent (added 1.2.39).
- **Removing the persistent `conf-file=` line** from `/jffs/configs/dnsmasq.conf.add` must be robust: a `grep -vF … && mv` skips the `mv` when our line is the *only* line (`grep -v` → 0 lines → exit 1) — leaving a dangling `conf-file` that makes firmware dnsmasq fatally fail at next boot (no DNS/DHCP, survives reboot).
- **A `conf-file=` include pointing at /opt (removable USB) is a LAN brick when /opt is absent** — our persistent include lives in firmware-owned `dnsmasq.conf.add` but its target is on /opt; when the USB is pulled/dying OR just not mounted yet at early boot, dnsmasq treats the missing `conf-file=` as FATAL and won't start (LAN-wide DNS/DHCP loss, survives reboots). The `dnsmasq.postconf` hook self-guards: on every dnsmasq (re)start it `pc_delete`s our `conf-file=` from the generated conf when the /opt target is missing (added 1.2.42). The hook is rewritten idempotently on every `install_page` (`sed '/# amneziawg/d'` then re-append) so upgrades gain new guards. NB: this brick is NOT OOM — a 256MB RT-AC68U "unreachable when AWG connects" was this dangling include, not memory pressure (multi-agent-verified: healthy free RAM, sequential downloads, `do_start` doesn't call `ensure_geo`).
- A `do_stop` that reloads dnsmasq **in the background** can race a packaging `rm -rf /opt/amneziawg` (uninstall/update) and OOM a low-RAM box — wait for the reload to settle before removing files.
- **"DNS via tunnel" (`awg_tunnel_dns=1`, off by default; uses the `awg_dns` field, ≤3 IPv4)**: appends a marker block (`# AWG_TUNNEL_DNS_START…END`: `no-resolv` + `server=<ip>@awg0`) to `dnsmasq_awg.conf` and touches `/tmp/.awg_tunnel_dns`, which makes the `dnsmasq.postconf` hook strip the firmware upstreams — so the LAN resolves ONLY through the tunnel (`@awg0` binds the upstream socket to awg0; the prio-100 `from <awg0-ip>` rule routes it). **INVARIANT: flag ⇔ block.** A stray flag without the `server=` lines leaves dnsmasq with NO upstreams (dead LAN DNS) — every conf-removal path (cleanup_firewall, reload_dnsmasq last-resort, `--test`-reject branch, uninstall, prerm) must clear the flag; `disable_tunnel_dns()` is the fail-open (called wherever `cleanup_dns_interception` fail-opens). Intercept-gated: inert in compat mode.

## Release & coexistence

- **Always add the `### X.Y.Z` CHANGELOG.md entry BEFORE tagging.** `release.yml` pulls release notes from it and the in-app update modal reads it; a tag without its entry ships a "naked" release.
- **api.github.com is region-blocked for the maintainer** — both `install-online.sh` and the in-app updater resolve the **version** GitHub API → **jsDelivr** data API → jsDelivr-mirrored `build-ipk.sh` `PKG_VERSION` (in that order). But **SHA256 is taken from the GitHub API release digest and SKIPPED when the API is blocked** (install proceeds unverified) — jsDelivr is NOT a checksum fallback (it mirrors only git-tracked repo files, not release assets). Don't claim jsDelivr verifies integrity.
- **Co-resident DPI/proxy tools** (zapret/zapret2, xray/XRAYUI, b4) share the router's `:53`, NFQUEUE, and route-table space. Running two NFQUEUE DPI tools at once can blackhole the box; the addon detects them and defaults «Режим совместимости» (no DNS hijack) on for new installs. `RT_TABLE=300` avoids colliding with them. This `coexist_warn` is about **AWG stealing the proxy's traffic** (fix via policy / compat mode).
- **Reverse coexistence — a transparent proxy stealing AWG's egress**: XRAYUI/xray in TPROXY "redirect all traffic" mode captures the router's OWN egress (incl. amneziawg-go's handshake UDP + the `awg0` health probe), so the tunnel comes **up but passes no traffic** and the health check rolls it back. `xray_redirect_active()` (gated `pidof xray` + (`TPROXY` in mangle OR `from all fwmark 0x10000` in `ip rule`), so the firmware WG-client's own fwmark rules don't false-alarm) → status field `xray_capture` → a red UI banner with the fix (disable XRAYUI redirect-all / exclude `awg0`+endpoint / run one VPN at a time). Opposite direction from `coexist_warn`.
- **Firmware VPN client (wgc*/VPN Fusion) can outrank AWG routing** — VPN-Fusion-style rules sit at ip-rule prio <98 (`20: from all lookup 8437`), so a CONNECTED client captures traffic before our fwmark rule. `fw_vpn_client_state()` → status `fwvpn_state`/`fwvpn_detail` → red/yellow UI banner + journal + diag section. Probe semantics (each clause field-tested): "active" = an UNCONDITIONAL from-all rule at prio 1–96 whose table holds a default route (fwmark-gated rules excluded — that's xray_capture's territory); "enabled" (yellow) = a NUMBERED `wgc1..5_enable=1` profile with NO interface up. **The bare unit-less `wgc_enable` nvram key on gnuton/stock builds is VPN Fusion's edit-buffer leftover, NOT a profile — it false-alarmed with everything visibly off (1.2.38 fix); never read it.** An enabled profile with its iface up routes via VPN Director rules (prio >10000 — below ours) and stays silent; real capture turns red via the rule scan regardless of source.
- **Broadcom CTF (hardware NAT accel) is INCOMPATIBLE with our policy routing — it doesn't just bypass the rules, it HANGS the box** (found 1.2.56). On CTF boxes (BCM470x — RT-AC68U and kin, the old CTF where `nvram ctf_fa_cap=0`, no runtime `fc` tool) the Cut-Through Forwarding flow-cache short-circuits netfilter/routing, so standing up our `ip rule`s (prio 97-100) + fwmark 0x100 corrupts the accelerator's kernel state → the router wedges and the **hardware watchdog reboots it** (field: remote RT-AC68U kernel 2.6.36 — EVERY `do_start` rebooted it, ~3-min dark, endpoint-independent; looks exactly like a hang from outside). **This is the REAL cause of the "AC68U reboots when AWG connects" crash — NOT the 1.2.55 loopback-endpoint theory (a red herring; that `validate_endpoint` loopback guard is a fine input check but fixes nothing here).** Merlin itself disables CTF the same way for its own policy-routed VPN/QoS. Fix: `ctf_active()` (module `ctf` in `/proc/modules` AND `nvram ctf_disable != 1`) gates `do_start` on EVERY path (boot/UI/watchdog) — it refuses BEFORE any net op, logs + records an incident, so no crash; status `ctf_block` → red UI banner with a one-click button → `do_ctf_disable()` sets **`nvram ctf_disable_force=1`** (+`ctf_disable=1`); `commit`; reboot. **`ctf_disable=1` ALONE does NOT persist** — the firmware recomputes `ctf_disable=0` at boot from the features IT knows (our policy routing is invisible to it), so CTF comes back (field-confirmed 1.2.57: `ctf_disable` 1→reboot→0). `ctf_disable_force` is the persistent knob the GUI's LAN→Switch Control "NAT Acceleration=Disable" sets and the boot init respects (`rmmod ctf` too risky). After the reboot `ctf_active` is false and the policy-routing no longer wedges the box (CTF-off costs some NAT throughput — CPU forwards). **But on the AC68U the tunnel STILL won't pass traffic** — the deeper `sendmmsg` ENOSYS blocker (see the daemon section) is unfixed, so CTF-off only removes the crash, not the no-traffic. Don't sell "disable CTF → working tunnel" on 2.6.x boxes. CLI `ctf_status`/`ctf_disable`; diag has a CTF line. **NEVER "just try starting" a policy-routed tunnel on an untested CTF box** — that's the crash. `ctf_active` false on the whole AX fleet (no module), so their behavior is unchanged.
- **UI is bilingual RU/EN**, auto-detecting the firmware language (httpApi `preferred_lang`).
